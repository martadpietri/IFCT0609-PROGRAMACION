#include <stdio.h>
int main(void)
{
    int l,perimetro;
    printf("El lado del cuadrado:");
    scanf("%d",&l);
    perimetro=4*l;
    printf("El perimetro del cuadrado es: %d", perimetro);
    return 0;

}